# AI Prompt Log - PayFlow Database Performance

### Prompt 1: Bản chất của Non-SARGable Query và Full Table Scan
- **Prompt:** "Tại sao khi viết `WHERE YEAR(created_at) = 2026`, MySQL lại từ chối dùng B-Tree Index trên cột `created_at` dù index đã được đánh?"
- **Phân tích áp dụng:** B-Tree Index lưu trữ giá trị gốc theo thứ tự sắp xếp. Khi áp hàm `YEAR()` hoặc `MONTH()`, cơ sở dữ liệu không thể tính trước giá trị hàm nếu không duyệt qua từng dòng. Do đó nó bắt buộc phải chạy phép quét toàn bảng (Full Table Scan), biến câu truy vấn thành Non-SARGable.

### Prompt 2: Quy tắc chọn thứ tự cột trong Composite Index (Leftmost Prefix Rule)
- **Prompt:** "Khi tạo Composite Index cho 2 cột `(transaction_type, created_at)`, tại sao nên đặt `transaction_type` đứng trước thay vì `created_at`?"
- **Phân tích áp dụng:** Áp dụng nguyên lý tiền tố ngoài cùng bên trái (Leftmost Prefix). Cột lọc bằng phép so sánh bằng (`= 'DEPOSIT'`) phải đứng trước để thu hẹp không gian tìm kiếm, sau đó cột so sánh khoảng (`>=` và `<`) đứng sau sẽ tận dụng được phần còn lại của cây nhị phân để quét dải (Range Scan).

### Prompt 3: Sự khác biệt giữa 'Using index' và 'Using index condition'
- **Prompt:** "Trong cột Extra của lệnh EXPLAIN, 'Using index condition' khác 'Using index' (Covering Index) như thế nào?"
- **Phân tích áp dụng:** `Using index condition` (ICP) là MySQL lọc dữ liệu ở tầng Engine lưu trữ trước khi đọc dòng đầy đủ. Nếu muốn đạt tới `Using index`, cần tạo Covering Index chứa cả cột `amount`: `(transaction_type, created_at, amount)`, giúp truy vấn không cần đọc bảng gốc (Clustered Index/Data page).

- Chuẩn bị câu hỏi Vấn đáp (Bảo vệ Giải pháp với CTO)Trình bày thứ tự thực thi thực tế (Execution Order) của câu lệnh SQL? Tại sao WHERE lại chạy trước SELECT?Trả lời: Thứ tự chuẩn là: FROM/JOIN $\rightarrow$ WHERE $\rightarrow$ GROUP BY $\rightarrow$ HAVING $\rightarrow$ SELECT $\rightarrow$ DISTINCT $\rightarrow$ ORDER BY $\rightarrow$ LIMIT. Mệnh đề WHERE phải chạy trước SELECT để lọc bỏ các bản ghi không hợp lệ ngay từ đầu, giảm tải lượng dữ liệu cần nạp vào bộ nhớ trước khi thực hiện các phép chiếu cột (projection) hay tính toán tổng (SUM, AVG) ở SELECT.Full Table Scan là gì? Khi nào Full Table Scan lại chạy nhanh hơn Index Scan?Trả lời: Full Table Scan là hành động đọc tuần tự toàn bộ các khối dữ liệu (data blocks) của bảng từ đĩa vào RAM. Nó sẽ chạy nhanh hơn Index Scan khi:Bảng có dung lượng rất nhỏ (vài trang dữ liệu).Truy vấn yêu cầu lấy tỷ lệ dữ liệu quá lớn (trên 20% – 30% tổng số dòng). Lúc này việc đọc tuần tự khối liên tục (Sequential I/O) sẽ có chi phí thấp hơn việc vừa đọc Index vừa nhảy đĩa ngẫu nhiên (Random I/O) để tra cứu dữ liệu gốc (Bookmark Lookup).Nếu bảng Transactions bị INSERT/UPDATE/DELETE liên tục hàng nghìn lần/giây, việc tạo thêm nhiều Index sẽ gây rủi ro gì?Trả lời:Suy giảm tốc độ ghi (Write Latency): Mỗi lệnh INSERT, DELETE hoặc UPDATE (vào các cột được đánh index) đều bắt buộc MySQL phải cân bằng lại cây B-Tree của tất cả các Index tương ứng.Phình to dung lượng ổ đĩa: Các file Index tiêu tốn tài nguyên bộ nhớ lưu trữ đáng kể.Tranh chấp khóa và bộ đệm (Buffer Pool Contention): RAM sẽ bị phân mảnh và chiếm dụng bởi các trang dữ liệu index thay vì phục vụ cache dữ liệu nóng cho giao dịch của khách hàng.
